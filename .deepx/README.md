# `.deepx/` — DEEPX Runtime Integration Knowledge

This is the **integration layer** for the DEEPX dx-runtime knowledge system.
It does NOT duplicate content from the sub-project knowledge bases. Instead,
it provides cross-project routing, unified agents, and integration-specific
instructions for tasks that span both dx_app and dx_stream.

---

## Architecture

| Level | Path | Scope | When to Use |
|---|---|---|---|
| **dx_app** | `dx_app/.deepx/` | Standalone inference apps (Python/C++) | Developing in dx_app submodule alone |
| **dx_stream** | `dx_stream/.deepx/` | GStreamer pipeline apps | Developing in dx_stream submodule alone |
| **dx-runtime** (this) | `.deepx/` | Cross-project integration | Working across dx_app + dx_stream |

Each sub-project `.deepx/` is fully self-contained and works independently
when the submodule is cloned alone. This integration layer adds:

- **Unified routing** — a single context routing table that covers all task types
- **Cross-project agent** — a router that dispatches to the correct sub-project builder
- **Integration instructions** — build order, shared model paths, cross-validation
- **Unified memory** — common pitfalls tagged by domain, spanning both projects
- **Unified scripts** — validators and generators that operate across all three `.deepx/` directories

---

## Directory Structure

```
.deepx/
├── README.md                          # This file — integration layer index
├── agents/
│   ├── dx-runtime-builder.md          # Unified router agent
│   └── dx-validator.md                # Unified validation orchestrator
├── instructions/
│   ├── integration.md                 # Cross-project integration patterns
│   └── agent-protocols.md             # Behavioral protocols (integration-scoped)
├── knowledge/
│   └── feedback_rules.yaml            # Validation finding → knowledge base mapping rules
├── memory/
│   └── common_pitfalls.md             # Unified pitfalls with domain tags
├── scripts/
│   ├── validate_app.py                # Unified app validator (dx_app + dx_stream)
│   ├── validate_framework.py          # Validates all 3 .deepx/ directories
│   ├── feedback_collector.py          # Collect validation findings into feedback proposals
│   └── apply_feedback.py              # Apply approved feedback fixes to .deepx/ files
├── skills/
│   ├── dx-agent-runtime-validate/
│   │   └── SKILL.md                   # Full validate → collect → approve → apply → verify loop
│   ├── dx-agent-brainstorm/
│   │   └── SKILL.md                   # Process skill — brainstorm and plan before code generation
│   ├── dx-agent-tdd/
│   │   └── SKILL.md                   # Process skill — test-driven development, validate incrementally
│   └── dx-agent-verify/
│       └── SKILL.md                   # Process skill — verify before claiming completion
└── templates/
    ├── en/                            # English instruction templates (.tmpl) — processed by dx-agent-gen
    └── ko/                            # Korean instruction templates (.tmpl) — processed by dx-agent-gen
```

> Platform file generation is handled by the suite-level **`dx-agent-gen`** CLI
> (canonical source is located at the suite root).

## Supported Tools

This integration-layer knowledge base is consumed by five AI coding tools:

- **Claude Code** — via `CLAUDE.md` plus on-demand `.deepx/` reads
- **GitHub Copilot** — via `.github/copilot-instructions.md`, `.github/agents/`, and `.github/skills/`
- **Cursor** — via `.cursor/rules/*.mdc`
- **OpenCode** — via `AGENTS.md`, `opencode.json`, and `.opencode/agents/`
- **Codex CLI** — via `AGENTS.md`, `.codex/skills/dx-codex-identity/SKILL.md`, and direct reads of `.deepx/skills/*/SKILL.md`

Codex CLI does not use the same `@mention` or slash-command wrappers as Copilot or
OpenCode at this layer. Instead, it relies on `AGENTS.md` for entry-point routing and
reads canonical `.deepx/` files directly when deeper grounding is required.

---

## Scripts — Audience and When They Run

The four scripts under `.deepx/scripts/` are **harness / knowledge-base
maintenance tools**, run by the engineer who develops and improves
`dx-agent-dev` itself. They do **NOT** fire automatically when an
end-user-facing application generated by an agent-driven CLI fails at runtime.

| Script | Run by | When | What it validates |
|---|---|---|---|
| `validate_framework.py` | KB maintainer / pre-commit hook | After editing any `.deepx/` file | Internal consistency of `.deepx/` (link drift, missing references, cross-level integrity) |
| `validate_app.py` | KB maintainer / E2E test harness | When inspecting a generated app artifact | Whether a generated app follows the patterns the KB documents (5 IFactory methods, `app.yaml` fields, GStreamer pipeline patterns, …) |
| `feedback_collector.py` | KB maintainer (manual) | Step 3 of the `DX Suite Validator` agent workflow | Aggregates the heterogeneous outputs of the validators above into a unified set of KB-update **proposals** (`feedback_report.json`) |
| `apply_feedback.py` | KB maintainer (manual, after approval) | Step 4 of the same workflow | Applies engineer-approved proposals (`--approve FB-001,…`) to the actual `.deepx/` files |

### Two scenarios — what is and isn't in this loop

**❌ Scenario A — End-user's generated app fails at runtime (NOT this loop)**

1. An end-user runs an app generated by an agent-driven CLI → it fails (e.g.,
   `setup.sh` forgot to create a venv, raising `ImportError`).
2. The session output under `dx-agent-dev/<session>/` keeps the failure log.
3. **None of these scripts fire automatically.**
4. The KB maintainer separately:
   - Aggregates failure patterns via the E2E autopilot analyzer
     (`.deepx/e2e/agent_analyzer/`), or
   - Reads issue reports manually,
   - Decides which `.deepx/` rule is missing or weak, then enters Scenario B.

**✅ Scenario B — KB maintainer audits the framework (this loop)**

1. Engineer edits something under `.deepx/skills/…` or `instructions/…`,
   then invokes the **DX Suite Validator** agent.
2. **Step 1** — `validate_framework.py` runs at all 3 levels → internal
   consistency check.
3. **Step 2** — `validate_app.py` runs against sample / reference apps →
   KB ↔ generated-app alignment check.
4. **Step 3** — `feedback_collector.py --all` → produces
   `feedback_report.json` with proposal IDs (FB-001, FB-002, …).
5. **Step 4** — Engineer reviews proposals and runs
   `apply_feedback.py --report … --approve FB-001,…` to apply only the
   accepted ones.
6. **Step 5** — Validators re-run to confirm the fix.
7. Finally, `dx-agent-gen generate` propagates the KB changes to the
   platform files (CLAUDE.md, AGENTS.md, copilot-instructions.md, etc.).

### Boundary

- ✅ **KB / framework integrity** — covered by these scripts.
- ✅ **Generated app compliance with KB rules** — covered by `validate_app.py`.
- ❌ **Automatic remediation of end-user runtime failures** — NOT part of
  this loop. End-user runtime failures feed in only indirectly through the
  E2E autopilot analyzer (`.deepx/e2e/agent_analyzer/`), where the
  maintainer manually decides which findings warrant KB changes.

---

## Unified Context Routing Table

Agents use this table to determine which sub-project knowledge base to load.
All paths are relative to the dx-runtime repository root.

| If the task mentions... | Sub-project | Read these files |
|---|---|---|
| **Python app, detection, factory** | dx_app | `dx_app/.deepx/skills/dx-agent-app-build-python/SKILL.md`, `dx_app/.deepx/toolsets/common-framework-api.md` |
| **C++ app, native engine** | dx_app | `dx_app/.deepx/skills/dx-agent-app-build-cpp/SKILL.md`, `dx_app/.deepx/toolsets/dx-engine-api.md` |
| **Async, high-throughput** | dx_app | `dx_app/.deepx/skills/dx-agent-app-build-async/SKILL.md`, `dx_app/.deepx/memory/performance_patterns.md` |
| **Model, download, registry** | dx_app | `dx_app/.deepx/skills/dx-agent-app-model-management/SKILL.md`, `dx_app/.deepx/toolsets/model-registry.md` |
| **GStreamer, pipeline, stream** | dx_stream | `dx_stream/.deepx/skills/dx-agent-stream-build-pipeline/SKILL.md`, `dx_stream/.deepx/toolsets/dx-stream-elements.md` |
| **MQTT, Kafka, message broker** | dx_stream | `dx_stream/.deepx/skills/dx-agent-stream-build-mqtt-kafka/SKILL.md`, `dx_stream/.deepx/toolsets/dx-stream-elements.md` |
| **Cross-project, integration** | dx-runtime | `.deepx/instructions/integration.md`, `.deepx/memory/common_pitfalls.md` |
| **Validation, testing** | Both | `.deepx/scripts/validate_app.py`, sub-project `instructions/testing-patterns.md` |
| **Validation, feedback, fix** | dx-runtime | `.deepx/skills/dx-agent-runtime-validate/SKILL.md`, `.deepx/knowledge/feedback_rules.yaml` |
| **ALWAYS read (every task)** | dx-runtime | `.deepx/memory/common_pitfalls.md` |

---

## Sub-Project Entry Points

| Sub-project | CLAUDE.md | .deepx/ |
|---|---|---|
| dx_app | `dx_app/CLAUDE.md` | `dx_app/.deepx/README.md` |
| dx_stream | `dx_stream/CLAUDE.md` | `dx_stream/.deepx/README.md` |

---

## Developer Workflow

```
1. Edit     →  Modify files in .deepx/ (this level or sub-projects)
2. Validate →  python .deepx/scripts/validate_framework.py
3. Generate →  dx-agent-gen generate   (or: bash .deepx/tools/scripts/run_all.sh generate from suite root)
4. Commit   →  git add .deepx/ && git commit
```

All changes flow from `.deepx/` outward. Never edit generated platform
files directly — they will be overwritten on the next regeneration.
